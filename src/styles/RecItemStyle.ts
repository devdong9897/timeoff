import styled from "@emotion/styled";

export const Rec = styled.div`
  .item-list {
    display: flex;
    img {
      position: relative;
      width: 250px;
      height: 300px;
    }
  }
  .prev-btn .next-btn {
    display: block;
    position: absolute;
    top: 500px;
    left: 50px;
  }
`;